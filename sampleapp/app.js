// setup application
angular.module('sampleapp', ['matchInput']);

// application controller
angular.module('sampleapp').controller('AppController', function($scope) {
    // application controller goes here
    //$scope.password = 'secret';
});
